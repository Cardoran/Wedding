[build]
  publish = ""
  functions = "functions"
  command = "npm install && npm run build"  # ← Wichtig!

[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"  # ← Beschleunigt den Build

[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200